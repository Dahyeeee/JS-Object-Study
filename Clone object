객체를 복제하면 같은 참조를 이용하게 되기 때문에 복제된 객체의 속성을 변경하면 원래 객체의 속성도 같이 변경된다.
같은 내용의 새로운 객체를 복제하고 싶다면 Object.assign({빈 새로운 객체},{복사하고자하는 객체},{여러개 복사 가능})로 얕은 복제를 한다.

객체 안에 또 객체가 있을 땐 얕은 복제로 속성공유를 막을 수 없다.
자바스크립트 라이브러리 lodash의 메서드인 _.cloneDeep(obj)을 사용하면 이 알고리즘을 직접 구현하지 않고도 깊은 복사를 처리할 수 있다.
